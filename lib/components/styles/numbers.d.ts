interface Props {
    hour24Mode?: boolean;
    inner?: boolean;
    active?: boolean;
}
export declare const numbersStyle: ({ hour24Mode, inner, active }: Props) => import("@emotion/utils").SerializedStyles;
export declare const numbersWrapperStyle: import("@emotion/utils").SerializedStyles;
export {};
